
/// Va cherche le contenu du fichier passer en paramètre et le retourne dans un string
/// @param fileName contient le nom de fichier utiliser par la fonction pour ouvrir le fichier correspondant
/// @return Contenu du fichier dont le nom est passé en paramètre ("fileName"), en un string

/// Chiffre et déchiffre le char donné selon la clé de chiffrement/déchiffrement voulue
/// @param text Caractère donné à remplacer
/// @param gap Clé de chiffrement et déchiffrement
/// @return Le nouveau caractère, post chiffrement/déchiffrement

/// Permets d'écrire un texte passé en paramètre dans un fichier passer lui aussi en paramètre
/// @param text Contenu a écrire dans le fichier.txt
/// @param fileName Nom du fichier de destination du contenu de la variable text

/// Permets d'établir les statistiques et les écrire
/// @param fileName Nom du fichier concerné par les statistiques

// Initialisation 1 car le fichier contient toujours au moins une ligne

// variables d'entrées de l'utilisateur

// Longueur du texte du fichier

// Caractère du tableau de char (string)

// Parse les chars du string